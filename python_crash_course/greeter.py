def greet_user(username):  # def is to inform python that I'm defining a
    # function
    """Display a simple greeting."""  # commeting called docstring
    print(f"Hello!, {username.title()}")  # the only thing that the function
    # does when called


greet_user('Diogo')
